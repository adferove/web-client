import React, { Fragment } from 'react';
import Landing from '../../components/Landing/Landing';

const home = () => {
  return (
    <Fragment>
      <Landing />
    </Fragment>
  );
};

export default home;
